fastmcp
# Pinned to match tutor-agent: langchain-mcp-adapters 0.3.1 cannot use mcp 2.x,
# and both sides of the MCP connection must speak a compatible protocol version.
mcp<2.0.0
boto3
pydantic
pytest
pytest-cov
moto[dynamodb,s3]
httpx
