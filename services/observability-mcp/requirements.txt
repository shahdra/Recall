fastmcp
# Pinned to match study-mcp and tutor-agent: langchain-mcp-adapters 0.3.1 cannot
# use mcp 2.x, and every MCP server in the repo should speak the same protocol
# version so one adapter version can talk to all of them.
mcp<2.0.0
boto3
httpx
pytest
pytest-cov
moto[s3]
